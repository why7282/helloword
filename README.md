# hello-springboot

一个最简单的 Spring Boot Hello World 示例项目。

## 运行要求

- 已安装 JDK 17（或兼容版本）
- 已安装 Maven（命令 `mvn` 可用）

## 运行项目

在项目根目录（包含 `pom.xml` 的目录）执行：

```bash
mvn spring-boot:run
```

启动成功后，在浏览器打开：

```text
http://localhost:8080/hello
```

你会看到：

```text
Hello World
```

